# Lens Drawing V3.4 版本更新说明

发布日期：2026 年 7 月 27 日

适用人员：镜片设计、工艺、出图、生产及检验人员

## 本次更新重点

### 1. 胶合面的 AD 可以分别填写

在“自定义出图”中选择双胶合或三胶合后，共用胶合面右侧新增“分离”复选框。

- 不勾选时，继续使用原来的共用 AD，操作方式与旧版本一致。
- 勾选后，可以分别填写胶合面左右两片镜片的 AD。
- 双胶合分离后，原有末端 AD 会自动顺延至 AD4。
- 三胶合的两个胶合面可以分别控制；两个胶合面都分离时，编号会自动顺延至 AD6。
- 取消分离不会删除已经填写的独立 AD，再次勾选后可以继续使用。

该功能仅用于“自定义出图”。批量 Excel/CSV 仍使用原来的 AD1 至 AD4 共用面格式。

### 2. 改善胶合镜片的 MD、AD 标注排布

软件会根据整个胶合镜片的外形自动安排 MD 和 AD 标注位置。多片镜片直径不同、口径不同或公差不同时，标注会自动错开，减少文字重叠和遮挡。

使用人员不需要额外设置，修改参数后查看预览即可。

### 3. 批量自定义参数会用于实际导出

在批量处理页面中，对某一条镜片记录使用“加工”进入自定义编辑并保存后，实际导出会优先使用该镜片的自定义值，不再被“参数设置”中的通用值覆盖。

例如：通用设置中的 N 为“自动计算”，某镜片的自动计算值为 2；若该镜片单独改为“手动输入 1.5”，则预览和最终导出的 PDF 都会显示 N=1.5。

本次统一检查并修复了以下逐行或逐页参数：

- N、ΔN、C、表面瑕疵 B 和署名。
- CA 自动系数、手动 CA 和逐片 CA。
- 自动倒角、手动倒角和逐片倒角。
- 厚度、矢高、定位直径及非定位直径公差。
- 玻璃厂商、玻璃品级和自定义镀膜明细。

自定义值会随当前批量会话保存。导出批量 Excel 时，软件会写入 `CustomProc` 列；重新导入该文件后，自定义值仍然保留。旧版本保存的会话和不含 `CustomProc` 列的旧模板仍可继续使用。

### 4. 无效参数提示更加明确

以下情况会在预览或导出前直接提示，不再生成可能误用的图纸：

- AD 大于所属镜片的 MD。
- 曲率半径无法覆盖对应 AD。
- 计算出的镜片边缘厚度小于或等于 0。
- 相邻胶合面的曲率不一致。
- CA、N、倒角、公差或其他数值为空、为负数或超出允许范围。
- 胶合镜片的第 2 片或第 3 片只填写了部分参数。

遇到提示时，请按提示中的镜片序号和左、右侧位置检查输入值。

### 5. 批量导出更安全

- 重复的 PartName 或 PartNo 会在导出前提示，避免同名 PDF 相互覆盖。
- 非法文件名、无效文件夹或超出所选目录的路径会被阻止。
- 批量导出失败时，界面会显示具体原因。
- 某一行参数错误时，只跳过该行，其余有效记录继续导出。
- Excel/CSV 导入失败后，临时文件会自动清理。
- 平面曲率 R=0 在导入编辑器时会保留，不再被当成空值。

### 6. 参数设置增加保护

全局参数设置中的空值、无效数值和不合理范围将无法保存。已有设置文件中若只有个别字段损坏，软件会仅恢复该字段的默认值，不影响其他有效设置。

## 升级后的兼容性

- 原有单片、双胶合和三胶合数据可以继续使用。
- 原有共用胶合面 AD 的出图方式保持不变。
- 原有批量 Excel/CSV 模板无需增加 AD5、AD6 列。
- 不含 `CustomProc` 列的旧批量模板仍可导入；从 V3.4 导出的 Excel 会自动包含该列。
- 旧版本中部分能够出图但几何条件无效的数据，在 V3.4 中可能会被拦截。这属于新增的图纸风险保护，请根据提示修正参数后再导出。

## 使用建议

1. 首次升级后，分别打开一组常用的单片、双胶合和三胶合数据检查预览。
2. 使用胶合面 AD 分离时，逐页检查各单片页面的左右 AD 是否与设计要求一致。
3. 批量导出前确认 PartName 和 PartNo 唯一，避免命名冲突。
4. 软件当前提供几何和输入合理性检查，不能代替材料、胶层、最小边厚、偏心、镀膜及实际加工能力评审。正式下发前仍需按企业工艺标准复核。
