# Lens Drawing V4.1 版本更新说明

发布日期：2026-08-18

## Agent 自动出图

- 中等置信 AD/MD、Automatic MEMA 或多个可行拓扑会生成固定 confirmation_id，必须取得用户消息或附件确认后才能执行。
- Agent 接口升级为 `4.1.0`，Request Schema 为 `1.2`，Task Schema 为 `1.1`。
- `create` 会生成 `source_analysis/geometry_cases.json`。检测到零厚度虚拟胶合面或需要判断的 AD/MD 归属时，任务进入 `awaiting_geometry_resolution`。
- 新增 `resolve-geometry`。Agent 只能选择 Lens Drawing 给出的候选 ID，不能提交或改写 Glass、T、R、AD、MD 数值。
- 执行出图前会重新读取同一 ZMX、重建候选集合并核对快照，避免源文件或候选漂移。

## 加工默认值

- 自动任务使用部署时一次性批准的 `deployment_policy.json`。
- 每个任务复制并锁定策略及 SHA-256。用户未明确提出的加工字段直接使用策略默认值，不再逐任务询问。
- 用户明确提出的特殊要求仍按全局、镜片组或逐页字段记录，并保留消息或附件证据。
- GUI 个人设置仍与 Agent 默认值隔离。

## 视觉检查

- `review` 现在明确区分 `vision_agent` 与 `human_operator`，生产默认由外部轻量视觉模型检查。
- 视觉报告必须列出全部 PDF 和 contact sheet 的路径及 SHA-256；Lens Drawing 会重新计算后才接受。
- PDF 自动校验失败不能被视觉报告覆盖。`passed` 报告不能含未解决问题或不确定项。

## ZOS-API 与限制

- Agent 可从部署策略或 CLI 显式取得 ZemaxRoot 与 OpticStudio 安装目录，随后才使用环境变量或 HKCU 回退。
- 提取结果记录实际使用的 NetHelper、ZOSAPI DLL、OpticStudio 版本和许可证状态。
- 首期仍只支持 Sequential、单配置、1–3 片 Standard 球面/平面镜片；非零偏心/倾斜、复杂面型、4 片以上和真实非零胶层继续阻断。
