# 更新日志

## V3.5 (2026-08-13)

面向软件使用人员的说明请参阅 [V3.5 版本更新说明](V3.5_版本更新说明.md)。

### 镀膜与加工要求
- 镀膜预设 `SQ-A3` 更新为 `SQ-A6`；历史设置、批量会话及 Excel `CustomProc` 中的旧值自动迁移
- 玻璃品级改为可输入下拉框，保留常用预设并允许手动填写任意文本
- 新增可编辑的 `Scribe&Break/Molding` 值，并支持通用设置、批量行和胶合逐页覆盖
- 胶合内表面支持按面选择“空白 / 蓝宝石膜”，双胶合和三胶合均可多选；选择结果随批量会话和 `CustomProc` 往返保存
- 被选中的胶合内表面合并镀膜表单元格并显示“蓝宝石膜”，外表面预设及自定义镀膜逻辑保持不变

### 胶合图纸版式
- 胶合整体页删除无实际内容的镀膜表及 N、ΔN、B 空行，仅保留左下 C 表和原右侧项目栏
- CT、ET、左右矢高及胶合总中心厚度的水平托线统一按渲染文字 BBox 计算，修复托线过短和同层端点不齐
- “蓝宝石膜”使用 Windows 中文字体回退并嵌入 PDF TrueType 字形

### 预览编辑体验
- 预览编辑由覆盖输入框改为“版面热区 + 浮动编辑器”，热区按图纸文字槽和表格单元格定位，不再依赖字符宽度
- 默认保持图纸干净；悬停时提示可编辑区域，开启“显示输入框”后统一显示全部热区，蓝宝石膜不再常驻显示
- 点击字段后在附近弹出固定尺寸编辑器，支持 Enter 保存、Esc 取消、连续字段编辑和胶合页签保持
- 窄屏使用最近字段命中容差，避免相邻 Ranking、Molding 等字段热区重叠或串行
- CA 浮动编辑器只编辑数值，不再将 `S1 φ` / `S2 φ` 前缀写入参数

### 批量与兼容性
- 自定义出图与批量“编辑出图参数”共用同一套编辑、保存和导出逻辑
- 继续遵循“逐页覆盖 > 批量行覆盖 > 通用设置”的参数优先级
- 单片镜头自动清除无效蓝宝石膜面；双胶合和三胶合仅保留结构中存在的内表面
- 新增迁移、参数优先级、Excel 往返、胶合镀膜渲染、热区几何和标注布局回归测试

## V3.4 (2026-07-27)

面向软件使用人员的说明请参阅 [V3.4 版本更新说明](V3.4_版本更新说明.md)。

### 自定义出图
- 胶合共用面 AD 可独立分离；双胶合支持顺延至 AD4，三胶合两个界面均分离时支持顺延至 AD6
- AD 行按实际表面顺序动态编号，取消分离时保留已输入的独立数值
- MD/AD 侧边标注改用组装体全局物理轨道，消除不同 MD 缩放造成的标注重叠

### 批量自定义参数
- 修复逐行自定义值在实际导出时被通用设置覆盖的问题；逐页覆盖、逐行覆盖、通用设置按明确优先级合并
- N、CA、倒角、公差、厂商、品级、C、B、ΔN、署名和自定义镀膜均使用同一预览/导出参数源
- Excel 新增 CustomProc 往返列，旧会话短键自动迁移，合法平面曲率 R=0 不再被当作空值
- 单行参数异常只影响该记录，其余有效记录继续导出

### 工程校验与可靠性
- 胶合预览、导出和批量流程统一校验有限数、AD/MD、曲率覆盖、边缘厚度和胶合曲率连续性
- 拦截负公差、无效 CA/N/倒角参数及损坏的持久化设置
- 批量导出防止重复名称覆盖、非法 Windows 路径和目录越界；失败原因改为界面可见
- 批量导入采用唯一临时文件并确保异常后清理
- 本地接口限制为回环地址和同源写请求，批量编辑 iframe 消息校验来源

### 回归测试
- 新增胶合标注 BBox、AD 分离组合、多页预览、输入校验、设置校验和批量输出安全测试

## V3.3 (2026-06-10)

### 参数传递与同步修复
- 单片 CA 手动模式：手动设置的净口径值正确传递到 PDF 导出，修复 ca_ratio 被忽略问题
- 镀膜自定义同步：通用设置镀膜参数同步到批量出图逐行编辑和自定义出图模块
- 批量出图逐行编辑新增镀膜预设方案选择器
- 胶合镜片逐片倒角：新增逐片手动设定倒角功能（双胶合/三胶合各镜片独立配置）
- 默认值对齐：统一镀膜参数各模块默认值，修复批量出图 CA 系数默认值偏差

## V3.2 (2026-06-10)

### 自定义出图
- 胶合镜片逐片 CA 手动输入（每片独立设置自动/手动模式）
- 胶合镜片各单页加工参数逐页隔离（overlay 编辑只影响当前页，不影响其他页）
- 预览 overlay 中 ranking（玻璃品级）改为下拉选择，与侧边栏一致
- CA 自动计算系数默认值从 0.98 调整为 0.94
- 修复 iframe 嵌入模式下参数加载竞态条件（采用 iframe-ready 握手机制）

### 批量处理
- 逐行加工参数持久化（保存后再次打开编辑器不再丢失）
- 进度条状态修复（不再提前显示"全部成功"）
- PartName/PartNo 自动填充支持 Enter 键触发覆盖模式
- Excel 导入时自动填充存档/编码 PDF 全局输入框
- 加工列位置调整至 AD4 和类型之间

### 界面
- 新增应用图标（pywebview 窗口图标 + 安装程序图标）

### 代码质量
- 数值型加工参数防御性类型转换（防止 JSON 反序列化后字符串类型错误）

## V3.1 (2026-06-08)

### 自定义出图
- 修复预览输入框与参数文字位置偏差问题
- 采用 matplotlib BBox 精确提取文字像素位置，实现像素级对齐
- 增大输入框点击区域，优化交互体验

## V5.0 (2026-06-05)

### 架构升级
- 从 Tkinter GUI 迁移至 PyWebview + Flask Web 架构
- 前端使用原生 HTML/CSS/JS，支持实时预览

### 自定义出图
- 支持单片/双胶合/三胶合镜片参数化绘制
- 实时预览（参数修改后自动刷新）
- 预览图上可编辑覆盖层（点击数值直接修改）
- 多页预览切换（胶合镜片：组装图 + 各单片页）
- PDF 导出
- 新增镀膜预设方案选择（SQ-A1/SQ-A6/SQ-A5/自定义）

### 批量处理
- Excel/CSV 批量导入（兼容亿赛通加密环境 COM fallback）
- 批量 PDF 导出（Save PDF + Mfr PDF 双文件夹）
- 新增：导出时同步生成可重新导入的 Excel 表格
- 新增：PartName 自动填充（编码PDF + 序号）
- 新增：PartNo 料号输入框与自动递增填充
- 新增：表格数据 session 持久化（导航离开后返回数据不丢失）
- 新增：逐行个性化出图参数（iframe 嵌入自定义出图模块，支持实时预览）

### 参数设置
- 加工要求（C/B/N/ΔN/署名/品级）
- 公差设置（定位/非定位直径公差、厚度/矢高公差、胶合定位基准）
- 镀膜默认参数（预设方案 + 自定义波段/反射率/角度）
- 倒角设置（自动/手动模式）
- CA 自动计算系数
- 绘图参数（字体/箭头/标注偏移）

### 代码质量
- 清理约 750 行废弃 Tkinter 代码
- 删除 12 个过时测试文件和 6 个旧构建脚本
- 修复双胶合/三胶合导出失败问题（cemented_ref_lens 类型转换）
- 改善导出错误提示（界面弹窗显示失败详情）
- 消除 settings 回退值隐患（.get fallback → 直接键访问）
- 新增 requirements.txt
