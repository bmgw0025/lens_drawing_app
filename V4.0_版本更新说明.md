# Lens Drawing V4.0 版本更新说明

发布日期：2026-08-14

## 主要更新

- 保留原有人工单片绘图、批量绘图和设置界面。
- 同一个 `LensDrawing.exe` 新增 `--agent` 接口，可由 AI Agent 创建、校验、执行、续接和视觉验收自动出图任务。
- 安装目录附带 `skills\lens-drawing-agent`，可安装到 Codex Skill 目录；Skill 每次调用都会校验自身 spec 与 EXE spec 是否一致。
- Agent 使用只读 ZOS-API 读取一个 ZMX，不修改源文件，并保存源文件哈希、已求值表面数据、几何来源和任务状态。
- 支持单片、双胶合和三胶合。零厚度非玻璃虚拟面在前后面型/曲率一致且无倾斜偏心时会折叠为同一个胶合界面。
- `H-K9L` 双平面单片组按业务规则识别为不需出图的棱镜，在交付清单中说明剔除原因和原始证据。
- 业务命名支持镜头型号、镜片型号和首枚生产编码：有效镜片组按表面顺序生成镜片型号及生产编码，剔除棱镜不占序号。
- 每个有效镜片组同时生成存档 PDF 和编码 PDF，并自动验证页数、字段、几何尺寸、加工要求、渲染像素和页面边界。
- 交付目录新增完整默认加工要求、特殊覆盖、有效值、几何警告和棱镜剔除清单。

## 默认值隔离

- GUI 人工设置仍会按 Windows 用户保存到 `%LOCALAPPDATA%\LensDrawing\app_settings.json`。
- Agent 每个新任务始终从 V4.0 内置固定默认值开始，绝不读取 GUI 上一次保存的特殊要求。
- 用户未提及的加工字段使用固定 Agent 默认值；用户明确提出的内容才作为特殊覆盖，并保留证据。

## Agent 入口

安装版使用统一 JSON 输出：

```powershell
LensDrawing.exe --agent --output-json "C:\work\result.json" spec
LensDrawing.exe --agent --output-json "C:\work\create.json" create "C:\work\lens.zmx" "C:\work\task"
```

推荐从安装目录取用 Skill，并通过：

```powershell
powershell -ExecutionPolicy Bypass -File ".\skills\lens-drawing-agent\scripts\Install-LensDrawingAgentSkill.ps1"
```

## 精度与限制

- 准确自动几何以 `.zmx` 和本机有效的 Ansys Zemax OpticStudio 2022 R2.01 ZOS-API 许可证为前提。
- 截图只能作为识别或需求证据，不能达到 ZMX 已求值数据的同等自动接受精度。
- 中等置信机械值必须在运行前按原值确认；明显低置信、非顺序、复杂面型、多配置或冲突数据会阻断任务。
- 完成状态同时要求自动 PDF 验证和 Agent 逐页视觉验收通过。
