# Lens Drawing V3.5 版本更新说明

发布日期：2026 年 8 月 13 日

适用人员：镜片设计、工艺、出图、生产及检验人员

## 本次更新重点

### 1. 镀膜预设 SQ-A3 更新为 SQ-A6

软件中原有的 `SQ-A3` 预设已统一替换为 `SQ-A6`。

- 参数设置、自定义出图和批量编辑器中的可见预设均显示 `SQ-A6`。
- 历史设置、旧批量会话及 Excel `CustomProc` 中保存的 `SQ-A3` 会在加载时自动转换为 `SQ-A6`。
- `SQ-A1`、`SQ-A5` 和自定义镀膜的使用方式不变。

### 2. 玻璃品级和 Molding 可以直接编辑

玻璃品级改为可输入下拉框：

- 可以继续选择 `01` 和 `00 ND±0.0003%  VD±0.3%` 两个常用值。
- 也可以直接输入客户或项目要求的任意玻璃品级文本。

`Scribe&Break/Molding:` 右侧内容也已开放编辑，默认仍为 `Molding`。

在批量处理中单独编辑某一行或胶合镜片某一单片页时，软件继续按以下顺序确定最终值：

1. 胶合逐页覆盖。
2. 批量行覆盖。
3. 参数设置中的通用默认值。

### 3. 胶合内表面可选择蓝宝石膜

双胶合或三胶合镜片的单片页中，胶合内表面的镀膜区域可以直接选择：

- 空白。
- 蓝宝石膜。

每个胶合内表面可以独立选择，允许一个胶合件中选择一个、多个或全部内表面。选择后，对应单片页会将 `Wavelength / Ravg / Angle` 两行三列合并，并在单元格中显示“蓝宝石膜”。

外表面的 `SQ-A1`、`SQ-A6`、`SQ-A5` 或自定义镀膜不受影响。胶合整体页不显示具体表面镀膜，因为该要求属于各单片页。

蓝宝石膜选择会随当前批量会话保存，并写入 Excel 的 `CustomProc`；重新导入后仍会保留。

### 4. 胶合整体页底部表格精简

胶合整体页中原来没有实际内容的表格已经删除：

- 删除中间的镀膜位置表。
- 删除左侧没有数值的 N、ΔN、B 行。
- 左下角仅保留胶合页 C 要求，并下移贴近页面底部。
- 右侧 Project、Signature、Date、Part No.、Part Name、Version、Material 和页码栏保持原位置及内容。

该调整仅影响胶合整体页，各单片页仍保留完整加工要求和镀膜表格。

### 5. 预览编辑改为类似 PDF 编辑器的操作方式

旧版预览将 HTML 输入框直接覆盖在图纸文字上，受字体宽度、缩放比例和屏幕尺寸影响，可能出现输入框与文字错位。本次改为“图纸热区 + 浮动编辑器”结构：

- 每个可编辑字段按照图纸中的文字槽或表格单元格建立精确热区。
- 默认状态下不显示输入框，图纸保持干净。
- 鼠标移动到可编辑字段时会显示临时高亮。
- 点击字段后，在文字附近弹出尺寸稳定的编辑器。
- 按 Enter 保存，按 Esc 取消。
- 可以从一个字段直接切换到另一个字段，前一个值会自动保存，完成后统一刷新预览。
- 胶合镜片编辑后保持当前镜片页，不再自动跳回整体页。

“显示输入框”按钮现在用于统一显示或隐藏全部编辑热区。蓝宝石膜与其他字段使用相同行为，不会再始终显示选择框。

在较窄的窗口中，软件会根据点击位置选择最近的字段，避免玻璃品级、Molding 等相邻行互相遮挡或选错。

### 6. 尺寸托线长度和对齐修复

CT、ET、左右矢高以及胶合总中心厚度的水平尺寸托线，改为根据实际渲染文字边界计算长度。

- 托线会超过文字边缘并保留固定间距。
- 同一侧的尺寸端点保持对齐。
- 不再使用字符数量和字号直接估算图纸坐标。

该修复适用于普通单片页和胶合镜片页面。

## 升级后的兼容性

- 原有单片、双胶合和三胶合镜片数据可以继续使用。
- 原有 `SQ-A3` 数据会自动迁移为 `SQ-A6`，无需手工修改历史 Excel。
- 不含 `CustomProc` 列的旧批量模板仍可正常导入。
- `proc_ranking` 仍为文本字段，旧玻璃品级值不会被限制或删除。
- 单片镜头不会保存胶合内表面蓝宝石膜选择；从三胶合切换为双胶合时，不存在的第三片表面会自动清除。
- PDF 文件结构、Save PDF / Mfr PDF 双目录和原有几何参数格式保持不变。

## 使用建议

1. 升级后先使用一组常用双胶合或三胶合数据，逐页检查外表面镀膜和胶合内表面蓝宝石膜。
2. 点击“显示输入框”可查看所有可编辑热区；正式检查图纸时可关闭该模式，避免界面标记干扰。
3. 对玻璃品级、Molding 或 CA 等内容连续编辑时，可以直接点击下一个字段，软件会自动保存前一个值。
4. 批量导出后建议抽查胶合整体页、各单片页及 `CustomProc` 回导结果。
5. 软件提供输入、几何和排版检查，但不能替代材料、镀膜工艺、胶合工艺及企业质量标准评审。正式下发前仍需人工复核。
